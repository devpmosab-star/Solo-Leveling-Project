import { portfolioProjects,getPortfolioProgress } from '../data/portfolioProjects'
export default function Portfolio({completedCareer}){return <section className="grid"><div className="card wide"><p className="kicker">Portfolio Projects</p><h3>مشاريعك العملية للملف المهني</h3><p className="muted">كل مشروع هنا يجب أن يصبح قابلًا للشرح في مقابلة.</p></div><div className="portfolio-grid">{portfolioProjects.map(p=>{const progress=getPortfolioProgress(p,completedCareer);return <div className="card portfolio-card" key={p.id}><span>{p.track}</span><h3>{p.title}</h3><div className="bar big"><div style={{width:`${progress}%`}}/></div><p>التقدم: {progress}%</p><div className="project-steps">{p.steps.map((s,i)=><small key={s}>{progress>=((i+1)/p.steps.length)*100?'✓':'☐'} {s}</small>)}</div></div>})}</div></section>}
