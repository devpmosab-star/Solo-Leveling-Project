import { careerItems,isCareerItemAvailable } from '../data/careerRoadmap'
import { timelineStages } from '../data/timeline'
export default function CareerRoadmap({careerReadiness,missingCareerItems,activeTimelineStage,completedCareer,completeCareerItem}){return <section className="grid"><div className="card wide"><p className="kicker">جاهزية Junior Automation Engineer</p><h3>{careerReadiness}%</h3><div className="bar big"><div style={{width:`${careerReadiness}%`}}/></div><p className="muted">الهدف: PLC + SCADA + BMS + مشاريع عملية + استعداد للمقابلات.</p><div className="missing-box"><b>المتبقي الآن:</b>{missingCareerItems.map(i=><span key={i.id}>☐ {i.title}</span>)}</div></div><div className="card wide timeline-card-v3"><p className="kicker">Career Timeline</p><div className="timeline-v3">{timelineStages.map(s=><div className={'timeline-step-v3 '+(s.id===activeTimelineStage?'active':'')} key={s.id}><b>{s.title}</b><small>{s.subtitle}</small></div>)}</div></div><div className="roadmap">{[1,2,3,4,5,6].map(stage=><div className="roadmap-stage" key={stage}><h3>المرحلة {stage}</h3><div className="career-grid">{careerItems.filter(i=>i.stage===stage).map(item=>{const available=isCareerItemAvailable(item,completedCareer),done=completedCareer.includes(item.id);return <div className={'card career-item '+(done?'done':'')+(!available?' locked':'')} key={item.id}><span>{item.track}</span><h3>{item.title}</h3><p>{item.guide}</p><small>وزن الجاهزية: {item.weight}</small><button disabled={!available||done} onClick={()=>completeCareerItem(item)}>{done?'تم':available?'إنجاز':'مقفلة'}</button></div>})}</div></div>)}</div></section>}
